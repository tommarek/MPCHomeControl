[package]
name = "controller_protocol"
version = "0.1.0"
edition = "2021"
description = "Universal, language-agnostic controller protocol types (the MPC ↔ hardware-controller contract)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
