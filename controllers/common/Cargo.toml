[package]
name = "controller_common"
version = "0.1.0"
edition = "2021"
description = "Shared helpers for the hardware-controller crates (the Loxone UDP datagram sink)."

[dependencies]
anyhow = { workspace = true }
chrono = { workspace = true }
