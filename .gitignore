/target/
/proptest-regressions/

# runtime artifacts (never commit)
/data/
forecast_snapshots.json
ev_prefs.json
*.bak

# secrets: generated DSN/token env files (write-pg-env.sh writes pg.env next to itself, and
# run-container.sh reads the InfluxDB token from a .env) — never commit, whatever the directory
pg.env
.env
*.env
!*.example.env
!scraper.example.json5

# python bytecode
__pycache__/
*.pyc

# local-only project guide — holds real hosts/paths/IPs + the deploy runbook (never commit)
/CLAUDE.md

# mqtt-bridge local config (copied from bridge.example.json5)
adapters/mqtt-bridge/bridge.json5
