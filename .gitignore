/target/
/proptest-regressions/

# runtime artifacts (never commit)
/data/
forecast_snapshots.json
ev_prefs.json
*.bak

# python bytecode
__pycache__/
*.pyc

# local-only project guide — holds real hosts/paths/IPs + the deploy runbook (never commit)
/CLAUDE.md

# mqtt-bridge local config (copied from bridge.example.json5)
adapters/mqtt-bridge/bridge.json5
